<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_album_tile_album"
        android:layout_width="@dimen/album_height"
        android:layout_height="@dimen/album_height"
        android:layout_margin="1dp"
        android:background="@drawable/album_mask_selector"
        android:clickable="true"
        android:focusable="true"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/tv_album_tile_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLength="8"
        android:maxLines="1"
        android:singleLine="true"
        android:textColor="@color/colorBlack"
        android:textSize="@dimen/about_sign" />

</LinearLayout>