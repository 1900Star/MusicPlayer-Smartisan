<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.yibao.music.view.music.MusicView

        android:id="@+id/music_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </com.yibao.music.view.music.MusicView>

    <androidx.constraintlayout.widget.Group
        android:id="@+id/group_song_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:constraint_referenced_ids="iv_song_add_to_list,iv_song_add_to_play" />

    <ImageView
        android:id="@+id/iv_song_add_to_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="20dp"
        android:contentDescription="@null"
        android:src="@drawable/more_select_icon_addlist_down"
        android:visibility="gone"
        app:layout_constraintEnd_toStartOf="@id/iv_song_add_to_play"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_song_add_to_play"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="50dp"
        android:contentDescription="@null"
        android:src="@drawable/more_select_icon_addplay_down"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>
